PLANNER_PROMPT = """
너는 작업 계획을 수립하는 역할이다.

사용자의 요청을 분석하고,
작업을 수행하기 위해 필요한 정보와 함수를 판단하라.

다음 JSON 형식으로만 반환하라.

{
    "search_queries": [
        "검색에 필요한 질문 1",
        "검색에 필요한 질문 2"
    ],
    "required_functions": [
        "필요한 함수 이름"
    ],
    "filename": "생성할 파일명"
}

사용자 요청:
{user_request}

출력 디렉토리:
{out_dir}

현재 출력 디렉토리에 존재하는 파일:
{files}

사용 가능한 함수:
{functions}
"""


GENERATION_PROMPT = """
사용자의 요청을 수행하기 위한 최종 결과를 작성하라.

사용자 요청:
{user_request}

작업 계획:
{plan}

검색된 정보:
{context}

다음 조건을 지켜라.

1. 검색된 정보를 기반으로 작성한다.
2. 검색 결과에 없는 내용을 사실처럼 만들어내지 않는다.
3. 사용자가 요청한 형식에 맞게 작성한다.
4. 최종 결과에는 설명이나 메타정보를 붙이지 말고 결과물만 작성한다.
"""